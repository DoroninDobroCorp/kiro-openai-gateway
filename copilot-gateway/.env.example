# Copilot Gateway Configuration

# Your secret API key for gateway access
# Clients must pass this in Authorization header: "Bearer your_key"
PROXY_API_KEY=changeme_proxy_secret

# Port to run the gateway on (default: 8001)
PORT=8001
